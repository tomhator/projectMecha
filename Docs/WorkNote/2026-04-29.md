# 2026-04-29 작업 일지

## 작업 내용
- 프로젝트 초기 설정 (Godot 4.x)
- 기본 디렉토리 구조 및 설정 파일 추가 (`.gitignore`, `.editorconfig`, `.gitattributes`)
- 기획 문서 및 프로젝트 가이드라인 작성 (`GameDesignDocument.md`, `AGENTS.md`, `ARCHITECTURE.md`)
- Cursor 에디터 룰 및 VSCode 설정 추가

### [chore] 프로젝트 아키텍처 및 2D 환경 설정 최적화
- `ARCHITECTURE.md`, `README.md`를 실제 프로젝트 구조 및 2D 픽셀 아트 방향성에 맞게 수정.
- `project.godot`에서 Jolt Physics 제거 및 2D 픽셀 아트용 렌더링 설정(Nearest 필터링, Aspect 유지) 추가.
- `ARCHITECTURE.md`에 `Scripts/Autoload` 및 `Scenes/Entities` 구조 반영.

### [feat] 전역 시스템(AutoLoad) 초기 구현 및 버그 수정
- `EventBus.gd`: 전투, 부품 장착, 스탯 변경 등 전역 신호 정의 및 `parts_unequipped` 신호 누락분 추가.
- `GameState.gd`: 런 상태 관리 로직 구현 및 변수명 불일치(`core_payload` -> `core_max_payload`) 수정.
- `Resource` 데이터 모델(`CoreData`, `PartsData`, `SkillData`)과의 연동 확인 및 보정.

### [design] 기획 문서 기반 아키텍처 문서화
- `GameDesignDocument.md`의 메카 조립(4슬롯) 및 턴제 전투 규칙을 `ARCHITECTURE.md`에 명시적으로 반영.

## 특이사항 / 결정 사항

- **2D 픽셀 아트 전환**: 기존 3D 기반 설정을 모두 제거하고 2D에 최적화된 환경으로 전환함.
- **코드 일관성**: `GameState`에서 리소스의 `max` 값을 참조할 때 변수명을 통일하여 런타임 에러 방지.

## 다음 작업 예정

- [ ] `MechaEntity.gd` 및 `EnemyEntity.gd` 기본 로직 구현
- [ ] 전투 턴 매니저 기초 설계
- [ ] 2D 메카 조립 UI 프로토타입 제작
