---
tags: [project/project-mecha, document/todo]
date: 2026-05-18
---

# 기획 TODO — 2026-05-18

---

## TODO 1. 던전 게임 루프 기획 상세화

**대상 문서:** `Docs/GameDesignDocument.md`

현재 GDD에는 방 종류·보상 타입은 적혀 있지만, 실제 흐름(어떻게 이어지는지)이 빠져 있다.
`RewardManager`·`TurnManager`를 구현했는데 "런 종료 조건"과 "보스 방 진입 흐름"이 문서에 없는 상태.

### 작성해야 할 내용

- [x] 런 구조 다이어그램 (층 구성 — 일반 방 N개 → 엘리트 → 보스)
- [x] 방 전환 흐름 (전투 클리어 → 보상 확인 → 다음 방 확인/재조립 → 다음 방 선택)
- [x] 런 종료 조건 2가지 (코어 HP 0 = 실패 / 보스 처치 = 성공)
- [x] 보스 방 특수 규칙 (보상 지급 후 즉시 런 종료)
- [x] 크레딧 획득 시점 및 사용처 (전투 클리어 지급, 작업대 사용)

---

## TODO 2. 전투 후 처리 기획 확정

**대상 문서:** `Docs/PartsSystem.md` §6.4 (수리), `Docs/GameDesignDocument.md`

손상도 감소는 구현됐지만 "전투 종료 후 어떻게 되는가"가 미결 상태.
`durability == 0` 파츠의 스킬 잠금·Affix 비활성도 정책만 있고 구현은 안 됐다.

### 작성해야 할 내용

- [x] 전투 종료 후 내구도 회복 정책 결정 및 명시
  - 안 A: 전투 후 자동 전량 회복 (내구도 = 전투 중 압박 요소, 런 간 지속 안 함)
  - 안 B: 회복 없음, 작업대/수리 키트로만 회복 (자원 관리 요소로 활용)
- [x] `durability == 0` 파츠 처리 규칙 명시
  - 스킬 버튼 잠금 조건 (AP 부족과 구분되는 "파괴" 상태 표시)
  - Affix 비활성 타이밍 (파괴 즉시 vs 턴 종료 후)
- [x] 적 파츠 파괴 스킬 3종 상세 (파츠 저격·과부하 공격·EMP 충격) — 예고 UI 포함
  - 현재 `PartsSystem.md`에 설명만 있고 "예고 UI가 어떻게 보이는지" 미기술

---

## TODO 3. 전투 화면 플레이어 메카 표시 기획

**대상 문서:** `Docs/UI/CombatScreen.md`

현재 전투 화면에 플레이어 메카의 파츠 구성이 시각적으로 보이지 않는다.
파츠 저격 예고 UI를 구현하려면 슬롯을 화면에 표시하는 구조가 먼저 필요하다.

### 작성해야 할 내용

- [x] 플레이어 메카 슬롯 표시 방식 → **블록형 십자 레이아웃** (조립씬 미니어처, 52×44px×5슬롯)
- [x] 슬롯별 손상도 상태 색상 코딩 → 정상(초록) / 손상(주황) / 파괴(빨강+dim) / 빈슬롯(회색)
- [x] 파츠 저격 예고 시 해당 슬롯 하이라이트 → 노란 테두리 + Tween 깜빡임, 비타겟 어둡게
- [x] 파괴된 슬롯 표시 방식 → 반투명 빨간 오버레이 + "✕" 라벨
- [x] 화면 내 위치 → `CoreNameLabel` 아래, 기존 `PartsLabels` 텍스트 VBox 대체
- [x] **구현**: 전투 UI 우측하단 `턴 종료` 버튼 추가 (스킬 불가 상태 수동 턴 종료 대응)
- [x] **구현**: 메카 표시 패널 구현 (`MechIllustration` + 파츠 상태 HUD를 `CombatUI.gd`에 통합)
- [x] **구현**: `SkillData.target_slot` 필드 + `EventBus.enemy_snipe_preview_changed` 시그널 추가 (파츠 저격 예고용)

---

## TODO 4. UI 문서 동기화

**대상 문서:** `Docs/UI/CombatScreen.md`, `Docs/UI/HUD.md`

전투 UI가 많이 바뀌었는데 문서가 구현보다 뒤처진 상태.
`CombatScreen.md`는 stub 수준이고, `HUD.md` §6은 최신 구현과 안 맞는 내용이 남아 있다.

### 작성해야 할 내용

- [x] `CombatScreen.md` 보강 — §4 TurnLabel, §5.1 펜딩 스킬 흐름·자유 교체, §5.2 자기 대상 캐처 시각 명세
- [x] `HUD.md` §6 전투 화면 항목 → 요점 요약 + `[[CombatScreen]]` 링크로 축소
- [x] GDD §4.2 패시브 스킬 항목 제거 — 액티브 전용으로 명시, 타입 표 재정리

---

## 구현 리스트 (남은 작업)

- [x] `CombatUI.gd`: 적 카드 폭이 좁을 때 예상 피해/예고 라벨 줄바꿈(`autowrap_mode`) 처리
- [x] `CombatUI.gd`: 예고 라벨과 예상 피해 라벨 분리(2줄 표시) 반영
- [x] `CombatUI.gd`: AP 부족/파츠 파괴 등 비활성 스킬 버튼에 사유 툴팁 표시
- [x] 전투 예상 수치 계산: `Affix` 효과(예: 반격 본능) 반영
- [x] 자기 대상 선택 시 `PlayerColumn` 전체 강조 적용
