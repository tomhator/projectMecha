---
tags: [project/project-mecha, document/todo]
date: 2026-05-18
---

# 기획 TODO — 2026-05-18

---

## TODO 1. 던전 게임 루프 기획 상세화 ✅

**대상 문서:** `Docs/GameDesignDocument.md`

### 작성해야 할 내용

- [x] 런 구조 다이어그램 → §2.1 첫 런(빈손) / 이후 런(사전 조립) 흐름 + 10층 루프 순서
- [x] 방 전환 흐름 → 전투 클리어 → 드롭 확인 → 다음 방 확인(재조립 가능) → 진입
- [x] 런 종료 조건 → 코어 HP 0 = 실패 / 보스 처치 = 성공 + 결과 화면 12항목 (§2.5)
- [x] 보스 방 특수 규칙 → 크레딧 85~105 + 드롭 0~2개 후 즉시 런 종료
- [x] 크레딧 획득 시점·사용처 → §2.4 범위 확정, §7.3 작업대 비용 확정

**주요 결정**:

| 항목 | 결정 |
|------|------|
| 파츠 드롭 | 0~2개, 획득은 **전부** (선택 없음) |
| 인벤토리 가득 찰 때 드롭 | 가져가지 못함 |
| 내구도 회복 정책 | **안 B: 자동 회복 없음** |
| 파손된 파츠 수리 | 거점에서만 가능 (던전 내 불가) |
| 작업대 수리 비용 | 50% 회복 41cr / 100% 회복 79cr (코어 무관 고정) |
| 부품 강화 비용 | `(PARTS_VALUE_{희귀도} × 0.7) + 15` |
| 인벤토리 슬롯 | 16(4×4) → 24 → 28 (코어 업그레이드) |

---

## TODO 2. 전투 후 처리 기획 확정 ✅

**대상 문서:** `Docs/PartsSystem.md` §6.4, `Docs/GameDesignDocument.md`

### 작성해야 할 내용

- [x] 내구도 회복 정책 → **안 B 확정**: 전투 후 자동 회복 없음, 런 전체 자원 관리 (§6.4)
- [x] `durability == 0` Affix 비활성 타이밍 → **파괴 즉시** (§6.2)
- [x] 수리 수단 3단계 → 작업대(50%/100%) / 수리 키트(TODO) / 거점(파손 파츠) (§6.5)
- [ ] 적 파츠 파괴 스킬 3종 상세 (파츠 저격·과부하 공격·EMP 충격) → TODO 3 메카 표시 기획과 연계, 추후 작성

---

## TODO 3. 전투 화면 플레이어 메카 표시 기획 ✅ (기획 완료 / 구현 부분 완료)

**대상 문서:** `Docs/UI/CombatScreen.md`

### 최종 확정 설계 (블록형 → 일러스트형으로 재설계)

초기 방향이었던 블록형 십자 레이아웃은 **폐기**. 아래 구조로 재설계.

**① MechIllustration (PlayerColumn 내)**
- [x] 표시 방식 → SD 캐릭터 **파츠 레이어 합성** 일러스트 (오른쪽 방향)
- [x] 레이어 순서 → BACK(z0) → LEG(z1) → CORE(z2) → ARM_R(z3) → ARM_L(z4)
- [x] 일러스트에 손상 tint 없음 — 항상 깨끗하게 표시
- [x] 플레이스홀더 구현 → `ColorRect` 5개, 실제 아트 완성 시 `TextureRect` 교체

**② PartsStatusHUD (화면 좌하단 십자형)**
- [x] 배치 방식 → 조립씬과 동일한 십자형 (등 / 오른팔·코어·왼팔 / 다리), 32×32px
- [x] 손상도 색상 코딩 → 정상(초록 테두리) / 손상(반투명+주황) / 파괴(어둡게+빨간✕)
- [x] 플레이스홀더 구현 → `GridContainer` 3×3, `Panel` + 슬롯명 라벨

**③ 파츠 저격 예고 (미구현)**
- [ ] `SkillData.target_slot` 필드 추가 예정
- [ ] `EventBus.enemy_snipe_preview_changed` 시그널 추가 예정
- [ ] 타겟 슬롯 노란 테두리 + Tween 깜빡임, 비타겟 어둡게

---

## TODO 4. UI 문서 동기화 ✅

**대상 문서:** `Docs/UI/CombatScreen.md`, `Docs/UI/HUD.md`

- [x] `CombatScreen.md` 보강 — §4 TurnLabel 위치·형식, §5.1 펜딩 스킬 2단계 흐름·자유 교체 규칙, §5.2 자기 대상 캐처(녹색 점선 박스) 시각 명세
- [x] `HUD.md` §6 전투 화면 항목 → 요점 요약 + `[[CombatScreen]]` 링크로 축소
- [x] `HUD.md` §2.4/§3 인벤토리 그리드 5×6 → 4×4 반영
- [x] GDD §4.2 패시브 스킬 항목 완전 제거, 액티브 전용 명시·스킬 타입 표 재정리

---

## 미완료 / 다음 세션 TODO

- [ ] `AssemblyScene.gd` 인벤토리 슬롯 코드 5×6=30 → 4×4=16 수정
- [ ] `is_broken()` 파츠 스킬 버튼 `disabled = true` 처리 (CombatUI)
- [ ] Affix 전투 효과 구현 (현재 카드 UI 표시만)
- [ ] 메카 일러스트 실제 아트 교체 (`ColorRect` → `TextureRect`)
- [ ] 파츠 저격 예고 연동 (`SkillData.target_slot` + `enemy_snipe_preview_changed`)
- [ ] 거점 기획 (수리·강화 비용 확정)
