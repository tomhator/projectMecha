---
tags: [project/project-mecha, document/worklog]
date: 2026-05-18
branch: design/2026-05-18
---

# 작업 일지 — 2026-05-18

## 요약

> ARM 슬롯 방향·역할 재정의, PartsCatalog 타입 수정을 완료하고 PR #4를 올렸다.  
> 이후 기획 브랜치(`design/2026-05-18`)를 분리해 던전 게임 루프·전투 후 처리·전투 화면 메카 표시 3개 항목의 기획서를 작성했다.

---

## 기능 구현 작업 (→ PR #4, branch `claude/review-context-next-task-5E5op`)

### 1. ARM 슬롯 방향 및 역할 재정의

- **계기**: 메크가 카메라를 바라보므로 조립 씬에서 ARM_R(오른팔)이 화면 **왼쪽**에 위치해야 함.
- **변경 내용**:
  - `Scenes/Assembly/AssemblyScene.gd` `_build_sockets()` Row 2를 `[ARM_R][코어][ARM_L]` 순으로 수정.
  - `Docs/PartsSystem.md` §4 ARM 슬롯 위치 표현: "섀시 좌측 (메크 기준)" / "섀시 우측 (메크 기준)" 명시.
  - ARM_R = **주력 무장 슬롯** (총기류 4 + 근접무기 4). ARM_L = **보조 무장 / 방어 슬롯** (총기류 4 + 방어·반응 4).
- **파츠 카탈로그 변경** (`Docs/PartsCatalog.md`):
  - ARM_R 전용: 근접무기 4종 (`arm_r_vhf9`, `arm_r_ew4`, `arm_r_yb20`, `arm_r_rd9`)
  - ARM_L 전용: 방어·반응 4종 (`arm_l_cp40`, `arm_l_emf3`, `arm_l_gdclaw`, `arm_l_gdtendril`)
  - `leg_spearhead2` 파츠명: `강습 역관절 2각` → `강습 2각` (타입 재분류)

---

## 기획 작업 (branch `design/2026-05-18`)

### TODO 1. 던전 게임 루프 (`Docs/GameDesignDocument.md`)

**추가/변경된 섹션**:

- **§2.1 런 구조 다이어그램**: 첫 런(빈손 시작) vs 이후 런(인벤토리·장비 꾸리기 화면), 10층 루프 순서.
- **§2.2 층 구성 테이블**: 1~3층 일반, 4~5층 분기/작업대, 6~8층 일반+엘리트, 9층 작업대, 10층 보스.
- **§2.3 방 타입**: 일반(드롭 0~2개), 엘리트(드롭 0~2개), 보스(드롭 0~2개+크레딧 보상), 이벤트/작업대.
- **§2.4 크레딧 범위**: 일반 15~25, 엘리트 45~65, 보스 85~105.
- **§2.5 결과 화면 12개 항목**: 총 전투 수, 크레딧 수지, 파츠 획득/파손 수 등.
- **§4.4~4.6 조립·인벤토리**: 매 전투 후 재조립 가능(파츠 교체 시 파손), 인벤토리 16→24→28 슬롯.

**주요 결정 사항**:

| 항목 | 결정 |
|------|------|
| 파츠 드롭 | 전투 난이도에 따라 0~2개, 획득은 **전부** (선택 없음) |
| 인벤토리 가득 찰 때 드롭 | 가져가지 못함 (슬롯 낭비 전략) |
| 내구도 회복 정책 | **안 B: 자동 회복 없음**, 작업대/수리 키트로만 회복 |
| 파손된 파츠 수리 | 던전 탈출 후 거점에서만 가능 (던전 내 불가) |
| 작업대 수리 비용 | 50% 회복 41크레딧, 100% 회복 79크레딧 (코어 무관 고정) |
| 부품 강화 비용 | `(PARTS_VALUE_{희귀도} × 0.7) + 15` (희귀도 별 값은 거점 기획 시 확정) |

### TODO 2. 전투 후 처리 (`Docs/PartsSystem.md`)

- **§6.2 파괴 상태**: Affix 비활성 타이밍 → **파괴 즉시** (턴 종료 후 아님).
- **§6.4 전투 종료 후 처리**: 안 B 확정 — "전투가 끝나도 손상도는 자동 회복되지 않는다". 전투 중/던전 중/런 후 수리 수단 테이블 추가.
- **§6.5 수리 수단**: 작업대(50%/100%)/수리 키트(TODO)/거점 수리(파손 파츠) 세 단계로 구분.

### TODO 3. 전투 화면 플레이어 메카 표시 기획 (`Docs/UI/CombatScreen.md`)

- **§2.3 MechStatusPanel** 신규 작성.
- 표시 방식: **블록형 십자 레이아웃** (조립씬 미니어처, ARM_R 화면 좌·ARM_L 화면 우).
- 손상도 색상: 정상(초록) / 손상(주황) / 파괴(빨강+dim+✕오버레이) / 빈 슬롯(회색).
- 파츠 저격 예고: 타겟 슬롯 노란 테두리 + Tween 깜빡임, 비타겟 어둡게.
- 구현 필요: `SkillData.target_slot` 필드, `EventBus.enemy_snipe_preview_changed` 시그널 (미구현).

---

## 변경된 파일 목록

| 파일 | 변경 종류 |
|------|---------|
| `Scenes/Assembly/AssemblyScene.gd` | 수정 — ARM_R/ARM_L 화면 위치 교체 |
| `Docs/PartsCatalog.md` | 수정 — ARM 슬롯 설명·전용 파츠 교체, `강습 2각` 이름 수정 |
| `Docs/PartsSystem.md` | 수정 — ARM 위치, §6.2 파괴 즉시, §6.4~6.5 전투 후 처리 |
| `Docs/GameDesignDocument.md` | 수정 — §2.1~2.5 런 루프, §4.4~4.6 조립·인벤, §7.3 작업대 비용 |
| `Docs/UI/CombatScreen.md` | 수정 — §2.3 MechStatusPanel 신규 추가, §7 향후 작업 갱신 |
| `Docs/WorkNote/TODO-2026-05-18.md` | 신규 — 오늘의 기획 TODO 리스트 |
| `Docs/WorkNote/2026-05-18.md` | 신규 — 본 작업 일지 |
