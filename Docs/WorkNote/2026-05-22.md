---
tags: [project/project-mecha, document/worknote]
date: 2026-05-22
---

# 작업 일지 — 2026-05-22

## 작업 내용

### [feat] 전투 후 파츠 자동 드롭 보상 전환

- 전투 승리 시 격파한 적 수를 집계해 `DungeonManager`에 보상 컨텍스트로 저장.
- `RewardManager.generate_combat_drops()`를 추가해 적 1기당 0개 40%, 1개 50%, 2개 10% 확률로 파츠 드롭 개수를 굴림.
- `RewardScene` 전투 보상을 3개 중 1개 선택 방식에서 드롭된 파츠 전부 자동 획득 방식으로 변경.
- 상자와 조우 이벤트의 기존 보상 호출은 유지.
- 참조한 문서: [[GameDesignDocument]], [[UI/HUD]]
- 변경된 파일: `Scenes/Combat/TurnManager.gd`, `Scenes/Combat/CombatScene.gd`, `Scripts/Autoload/DungeonManager.gd`, `Scripts/Autoload/RewardManager.gd`, `Scenes/Dungeon/RewardScene.gd`, `Docs/GameDesignDocument.md`, `Docs/UI/HUD.md`, `Docs/TODO/TODO-NEXT.md`

### [chore] 자동 검증 v1 구축

- `Scripts/Validation/validate.sh`를 프로젝트 검증 진입점으로 추가.
- Godot 4 금지 문법 패턴, 프로젝트 문서, `gdparse`, Godot headless 로드 검사를 추가.
- 자동 검증 v2로 리소스 무결성 검사와 주요 씬 스모크 검사를 추가.
- `.claude/hooks/validate-project.sh`와 `.claude/settings.json` Stop 훅을 연결.
- `.vscode/settings.json`의 Godot Tools 경로를 `PROJECT_MECHA_GODOT4` 환경변수 기반으로 전환.
- macOS Godot 4.6.1, Windows Godot 4.6.2(`c:\Users\theoe\gameDev\Godot_v4.6.2-stable_win64.exe`) fallback 탐색을 추가.
- `skill_iron_shield.tres`의 중복 `skill_id`를 `215`로 정리.
- 참조한 문서: [[TODO/TODO-NEXT]]
- 변경된 파일: `Scripts/Validation/*`, `.claude/hooks/validate-project.sh`, `.claude/settings.json`, `.vscode/settings.json`, `Resources/Skills/skill_iron_shield.tres`, `ARCHITECTURE.md`, `Docs/TODO/TODO-NEXT.md`

### [fix] P0 전투 검증 항목 보강

- 파괴된 파츠 스킬은 버튼 표시 목록에는 남기고, 실제 사용 가능 목록에서는 제외하도록 `MechaEntity.get_display_skills()` / `can_use_skill()`을 추가.
- 사용 가능한 스킬이 없을 때 플레이어 턴이 자동 종료되도록 `TurnManager`를 보강.
- 수집가 팔 파괴 시 `enemies` 배열에서 제거하고 노드도 해제해 전투 타겟 목록과 런타임 객체가 남지 않도록 수정.
- 수집가 보스 `active_arms` 정리를 외부에서 호출할 수 있도록 `prune_defeated_arms()`를 추가.
- `Scripts/Validation/check_p0_combat_flows.gd`를 추가하고 `validate.sh`에 연결해 일반 전투, 파괴/Affix, BACK 저격, 수집가 팔/탈취/승리 조건을 headless 검증.
- 검증: `bash Scripts/Validation/validate.sh` PASS.
- 변경된 파일: `Scenes/Combat/TurnManager.gd`, `Scenes/Entities/MechaEntity.gd`, `Scenes/Entities/BossCollectorEntity.gd`, `Scripts/Validation/check_p0_combat_flows.gd`, `Scripts/Validation/validate.sh`, `Docs/TODO/TODO-NEXT.md`

## 특이사항 / 결정 사항

- macOS 파일시스템에서 `scripts/`가 기존 `Scripts/`와 충돌하므로 검증 스크립트는 `Scripts/Validation/`에 배치.
- `gdparse`, `godot`, `godot4`가 PATH에 없으면 로컬 후보 경로를 먼저 찾고, 없으면 실패 대신 `SKIP`으로 처리.
- P0 검증은 수동 플레이테스트 대신 재현 가능한 Godot headless 스크립트로 우선 완료 처리. 실제 UI 눈검수는 이후 데모 플레이 세션에서 별도 확인 가능.

## 다음 작업 예정

→ [[TODO/TODO-NEXT]]
