---
tags: [project/project-mecha, document/worknote]
date: 2026-05-21
---

# 작업 일지 — 2026-05-21

## 작업 내용

### [feat] 전투 저격·파괴 연동 + 수집가 보스 구현

- **Phase A (작업 0~5)**
  - 일반 적 스킬 5종 `target_slot` → NONE 초기화 (저격 오발 방지)
  - `MechaEntity`: 파괴 파츠 스킬 제외, Affix 무효화, `get_part_at_slot` / `steal_part_at_slot`
  - `EnemyEntity`: 저격 내구도 감소, `hit_count` 다중 타격, 빈 슬롯 예고 숨김
  - 저격 스킬 `.tres` 3종 생성 (`skill_snipe_arm`, `skill_overload_attack`, `skill_emp_shock`)
- **Phase B (작업 6~9)**
  - 수집가 전용 스킬·`enemy_collector.tres`, `DungeonManager` 보스 풀 교체
  - `CollectorArmEntity`, `BossCollectorEntity` (팔 4 스폰·재수집·팔 탈취·코어 격파 승리)
  - `EventBus`: `part_stolen`, `boss_arm_spawned`, `enemy_added`
  - `TurnManager.add_enemy`, `CombatScene` 보스/팔 스폰, `CombatUI` 강탈·적 추가 UI 갱신
- 참조: [[DEV-GUIDE-2026-05-21]], [[EnemySystem]]
- 변경: `Scenes/Entities/*.gd`, `Scenes/Combat/*.gd`, `Scenes/UI/CombatUI.gd`, `Resources/Skills/*`, `Resources/Enemies/enemy_collector.tres`

## 특이사항 / 결정 사항

- `PartsData` 필드명은 `parts_skills` (가이드의 `skills` 아님)
- 방어 팔 스킬은 `CollectorArmEntity`에서 코어 `apply_core_shield_heal` 호출로 보스 쉴드 회복
- 보스 `setup()`은 `super.setup()` 대신 팔 스폰 후 `decide_next_actions` (초기 재수집 오발 방지)

## 다음 작업 예정

→ [[TODO-NEXT]]
