---
tags: [project/project-mecha, document/worknote]
date: 2026-05-14
---

# 작업 일지 — 2026-05-14

## 작업 내용

### [feat] is_damaged 제거·durability 단일화 및 PartsFactory
- `PartsData`: `max_durability`/`durability`, `drop_weight`, `affix_pool`, `stat_multiplier`, `rolled_affixes`, `is_worn()`, `is_broken()`, `grade()` 추가. `is_damaged` 제거.
- `PartsFactory` autoload: 템플릿 `duplicate(true)` 후 `stat_multiplier` 롤, 드롭 등급별 affix 개수(COMMON 0~1, RARE 2~3, EPIC 4~5), `affix_pool`에서 중복 없이 affix ID 선택, `evolution_lord`는 BACK·ARM만, `max_durability`/등급 반영.
- `RewardManager.generate_choices`: 로드한 템플릿마다 `PartsFactory.generate(template, grade)` 호출.
- 조우 B: `durability -= 1` + `_normalize_durability()`. `MechaEntity`·조립 UI는 `is_worn()`으로 ×0.7·⚠ 표시.
- 참조 문서: [[PartsSystem]], [[AffixSystem]], [[GameDesignDocument]]
- 변경 파일: `Resources/PartsData.gd`, `Scripts/Autoload/PartsFactory.gd`, `Scripts/Autoload/PartsFactory.gd.uid`, `Scripts/Autoload/RewardManager.gd`, `project.godot`, `Scenes/Dungeon/EncounterScene.gd`, `Scenes/Entities/MechaEntity.gd`, `Scenes/Assembly/PartCardUI.gd`, `Scenes/Assembly/PartSocketUI.gd`

### [design] Phase16 보류 문서·GDD 정리
- `Docs/PartsSystem.md`: §6 이벤트/UI 문단, 슬롯 표에서 패시브 문구 정리, 변경 이력 0.5, §7.2 생성 흐름을 `drop_grade` 반영, TODO 일부 완료 표기, 32종·24→32 문구.
- `Docs/GameDesignDocument.md`: §4.1·§4.2 패시브 제거 방향, §4.3 32종 문구 정리, `updated` 갱신.

### [chore] 아키텍처 문서
- `ARCHITECTURE.md`: Autoload 목록에 `PartsFactory`, `PartsData` 설명 보강, §3.3에 PartsFactory 요약.

### [feat] 전투 화면 상단 중앙 턴 표시
- `Scripts/Autoload/EventBus.gd`: `signal combat_turn_changed(turn: int)` 추가.
- `Scenes/Combat/TurnManager.gd`: `current_turn: int` 추가. `start_combat`에서 0으로 초기화, `start_player_turn` 시작 시 +1 후 `EventBus.combat_turn_changed` emit. 콘솔 로그도 `[턴 N · 플레이어]`로 갱신.
- `Scenes/UI/CombatUi.tscn`: `RunStatusStrip`(0~56px) 바로 아래 60~92px에 `TurnLabel`(중앙 정렬, font 18) 추가. `BattleMargin` `offset_top` 64 → 96으로 이동해 겹침 방지.
- `Scenes/UI/CombatUI.gd`: `@onready var turn_label`, `_on_combat_turn_changed(turn)` 핸들러 연결. 라벨 텍스트 "턴 N".

### [fix] 전투 UI — 펜딩 스킬 자유 교체·자기 대상 클릭 영역 시각화
- `Scenes/UI/CombatUI.gd`:
  - `_set_skill_buttons_disabled`가 더 이상 버튼을 비활성화하지 않고 강조 토글만 수행. 펜딩 중에도 모든 스킬을 자유롭게 다시 선택 가능. 모드(ENEMY↔SELF) 전환도 기존 분기에서 정리됨.
  - `_style_skill_button_pending` 강조를 `disabled` 스타일박스 → `normal`/`hover`/`pressed` 스타일박스에 적용 (활성 버튼에서도 보이도록).
  - `_player_target_catcher`: 펜딩 SELF일 때 `flat=false`, "내 메카 클릭" 텍스트, 점선 박스 스타일을 표시. `_style_player_target_catcher_active`/`_clear_player_target_catcher_style` 도입. 평소(IGNORE 상태)에는 텍스트·스타일 모두 제거.
  - 회복 스킬(예: skill_repair)이 보이는 클릭 박스를 통해 발동되지 않던 문제 해결.

## 특이사항 / 결정 사항

- **Affix 개수**: `PartsSystem` §3.2 확률표 대신, 보상 폴더 등급과 맞추기 위해 `PartsFactory`에서는 COMMON/RARE/EPIC 드롭 시 각각 0~1 / 2~3 / 4~5개로 고정 범위 롤. (방 종류별 가중치는 미구현, PartsSystem TODO 유지.)
- **에디터 HUD 검증**: 이 환경에 Godot CLI가 없어 자동 실행 검증은 생략. 아래 체크리스트는 에디터에서 수동 확인 필요.

## HUD·맵 수동 검증 체크리스트 (SESSION_CONTEXT 2026-05-11)

- [ ] 던전 맵: 우측 패널·좌측 타일 정렬·긴 힌트 시 스크롤
- [ ] 전투: 적 2명 이상 시 가로 카드 너비·예고 라벨 줄바꿈·타겟 선택 UX
- [ ] 런 스트립: 해상도별 상단 높이·`10 / 100` 글자 잘림

## 다음 작업 예정

- [ ] 스킬 사용 시 `durability -= 1`, 파괴 시 스킬 잠금·Affix 비활성 (`CombatSpecification`/`TurnManager` 등)
- [ ] `affix_pool`을 각 `.tres`에 채우고 §3.2 확률표·방 가중치를 `PartsFactory`에 이식
- [ ] PartCardUI — 이름 접두사, 손상도 게이지, affix 아이콘
- [ ] 24→32 `.tres` (PartsCatalog)
