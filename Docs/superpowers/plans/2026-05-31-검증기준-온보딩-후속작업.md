# 검증 기준·온보딩 설계 후속 작업 구현 계획

> [!warning] ⚠️ 옛 코어 기준 (역사 기록) — 2026-06-03 코어가 "교체의 긴장" → "파츠가 주인공"으로 전환됨. 이 계획의 V1~V6·교체 딜레마·수락률 관련 구현/측정은 옛 코어 산물이다. 새 코어 기준은 [[superpowers/specs/2026-06-03-조립화면-재설계-design]] 참조. 완료된 계획이라 본문은 보존만 한다.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 설계 문서(`Docs/superpowers/specs/2026-05-31-검증기준-온보딩-안할것-design.md`) §4 후속 작업 4항목을 구현한다 — (1) TODO 반영, (2) GDD 링크, (3) 첫 런 2층 오른팔 교체 딜레마 보장 로직, (4) V5 교체 딜레마 빈도 headless 측정.

**Architecture:** 핵심은 첫 런 온보딩 보장이다. `ARM_R` 타입 파츠는 오른팔 소켓에만 들어가므로(타입 잠김, EXTRA_ARM은 첫 런에 잠김), 1·2층 전투 보상에 COMMON `ARM_R` 드롭을 보장하면 — 1층에서 오른팔을 채우고 2층에서 그 자리를 강제 교체(파손)하는 딜레마가 100% 성립한다. `RewardManager`에 온보딩 드롭 생성 메서드를 추가하고, `RewardScene`의 전투 보상 경로에서 첫 런 1·2층일 때 주입한다. 검증은 프로젝트의 기존 계약 테스트(`check_*.gd` + `validate.sh`) 패턴을 따른다.

**Tech Stack:** Godot 4.6 / GDScript. 검증: headless 계약 테스트 (`SceneTree` 스크립트). 표준 진입점 `bash Scripts/Validation/validate.sh`.

**검증 실행 (모든 태스크 공통):**
- 전체: `bash Scripts/Validation/validate.sh`
- 단일 스크립트 (빠른 반복): `"$GODOT" --headless --path . --script res://Scripts/Validation/check_current_work_contracts.gd`
  여기서 `$GODOT`는 `C:/Users/theoe/gameDev/Godot_v4.6.2-stable_win64.exe` (또는 `GODOT_BIN` 환경변수).

---

## Task 1: 설계 문서 반영 (TODO + GDD 링크)

순수 문서 작업. 코드 변경 없음. 먼저 처리해 설계와 추적 문서를 일치시킨다.

**Files:**
- Modify: `Docs/TODO/TODO-NEXT.md`
- Modify: `Docs/GameDesignDocument.md:456-466` (§10 미결 사항)

- [ ] **Step 1: TODO-NEXT.md에 검증·온보딩 섹션 추가**

`Docs/TODO/TODO-NEXT.md`의 `## P1 — 후속 구현` 섹션 바로 위(`---` 다음)에 아래 블록을 삽입한다:

```markdown
## P0.5 — 핵심 재미 검증 (플레이테스트)

> 기준 문서: [[superpowers/specs/2026-05-31-검증기준-온보딩-안할것-design]]
> 핵심 재미 = "교체의 긴장". 아래는 전달력·재미 검증(층위 2·3). 기능 검증(층위 1)은 `validate.sh`가 커버.

- [ ] **V1 교체 수락률 30~70%** — 점유 슬롯 교체 제안 대비 수락 비율. 플레이테스트 5런 집계. 밴드 벗어나면 파손 비용·드롭 등급 곡선 재조정(GDD §4.5/§2.3), 연속 2회 실패 시 교체 비용 모델 재검토.
- [ ] **V2 첫 교체 딜레마 3분 이내(2층)** — 신규 플레이어 관찰. (구현: Task 3 온보딩 보장)
- [ ] **V3 교체 결과 인지 80%+** — 교체 후 강해졌나/약해졌나 정답률. 실패 시 조립/교체 UI 비교 표시 개선, 3회 실패 시 "교체 전후 수치 1줄 요약" 강제 표시.
- [ ] **V4 한 화면 정보로 첫 교체 판단** — 추가 설명 없이 진행. 신규 플레이어 관찰.
- [ ] **V5 런당 교체 딜레마 3회+ / 4층 연속 0회 없음** — 플레이테스트 5런 집계. headless 측정은 `check_swap_dilemma_frequency.gd`(Task 4).
- [ ] **V6 "한 판 더?" 과반(3/5) yes** — 사후 질문. 실패 시 코어루프 기획 재진단. **이 통과 전까지 NOT DOING 목록 해제 금지.**
```

- [ ] **Step 2: GDD §10에 설계 문서 링크 추가**

`Docs/GameDesignDocument.md`의 §10 미결 사항 `> [!todo]` 블록 마지막 줄(`- [ ] 실제 아이콘/스프라이트 연결 및 조립 실루엣 고도화`) 다음에 한 줄 추가:

```markdown
> - [ ] 핵심 재미 검증 기준·첫 10분 온보딩·NOT DOING — [[superpowers/specs/2026-05-31-검증기준-온보딩-안할것-design]]
```

- [ ] **Step 3: 문서 정합성 확인**

Run: `bash Scripts/Validation/check_project_docs.sh`
Expected: 종료 코드 0 (문서 검사 통과). 링크 깨짐/형식 오류 없음.

- [ ] **Step 4: 커밋**

```bash
git add Docs/TODO/TODO-NEXT.md Docs/GameDesignDocument.md
git commit -m "docs: 검증 기준 V1~V6 TODO 반영 및 GDD 설계 링크 추가

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: RewardManager에 온보딩 오른팔 드롭 생성 추가

`ARM_R` 폴더에서 무작위 템플릿을 골라 COMMON 등급으로 생성하는 메서드를 추가한다. 이것이 보장 로직의 부품 공급원이다.

**Files:**
- Modify: `Scripts/Autoload/RewardManager.gd`
- Modify: `Scripts/Validation/check_current_work_contracts.gd`

- [ ] **Step 1: 계약 테스트부터 작성 (실패 확인용)**

`Scripts/Validation/check_current_work_contracts.gd`의 `_initialize()` 안 `_check_reward_scene_contracts()` 호출 다음 줄에 새 호출을 추가한다:

```gdscript
	_check_reward_scene_contracts()
	_check_onboarding_arm_guarantee()
```

그리고 `_check_reward_scene_contracts()` 함수 정의 바로 위에 아래 함수를 추가한다 (이 Step에서는 RewardManager 부분만, RewardScene 부분은 Task 3에서 채운다):

```gdscript
func _check_onboarding_arm_guarantee() -> void:
	var reward_manager: Node = root.get_node_or_null("RewardManager")
	if reward_manager == null:
		_fail("Missing RewardManager autoload")
		return
	for _i: int in range(40):
		var arm: PartsData = reward_manager.call("generate_onboarding_arm_drop") as PartsData
		_assert_true(arm != null, "Onboarding arm drop returned null")
		if arm == null:
			return
		_assert_true(arm.parts_type == PartsData.PartsType.ARM_R, "Onboarding drop is not an ARM_R part")
		_assert_true(arm.grade() == PartsData.PartsGrade.COMMON, "Onboarding arm drop is not COMMON grade")
	print("Current OK: onboarding ARM_R drop generation")
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `"$GODOT" --headless --path . --script res://Scripts/Validation/check_current_work_contracts.gd`
Expected: FAIL — `generate_onboarding_arm_drop`가 아직 없어 `Invalid call. Nonexistent function 'generate_onboarding_arm_drop'` 또는 null 단언 실패.

- [ ] **Step 3: RewardManager에 메서드 구현**

`Scripts/Autoload/RewardManager.gd` 상단 상수부 `DROP_COUNT_ONE_CHANCE` 다음 줄에 추가:

```gdscript
const ARM_R_DIR: String = "res://Resources/Parts/arm_r"
```

그리고 `generate_combat_drops(...)` 함수 정의 바로 위에 아래 메서드를 추가:

```gdscript
# 첫 런 온보딩 전용: 오른팔 소켓 유도용 COMMON 오른팔 파츠 1개를 생성한다.
# ARM_R 타입은 오른팔 소켓에만 장착되므로(EXTRA_ARM은 첫 런에 잠김)
# 1층=채움 / 2층=강제 교체 딜레마가 성립한다.
func generate_onboarding_arm_drop() -> PartsData:
	var paths: Array[String] = []
	var dir := DirAccess.open(ARM_R_DIR)
	if dir != null:
		dir.list_dir_begin()
		var file_name := dir.get_next()
		while file_name != "":
			if not dir.current_is_dir() and file_name.ends_with(".tres"):
				paths.append("%s/%s" % [ARM_R_DIR, file_name])
			file_name = dir.get_next()
		dir.list_dir_end()
	if paths.is_empty():
		push_warning("RewardManager: 온보딩 오른팔 템플릿을 찾을 수 없습니다")
		return null
	var template: PartsData = load(paths[randi() % paths.size()]) as PartsData
	if template == null:
		return null
	return PartsFactory.generate(template, PartsData.PartsGrade.COMMON)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `"$GODOT" --headless --path . --script res://Scripts/Validation/check_current_work_contracts.gd`
Expected: PASS — 출력에 `Current OK: onboarding ARM_R drop generation` 포함, 마지막 줄 `Current work contracts: PASS`.

- [ ] **Step 5: 커밋**

```bash
git add Scripts/Autoload/RewardManager.gd Scripts/Validation/check_current_work_contracts.gd
git commit -m "feat: RewardManager 온보딩 오른팔 드롭 생성 추가

첫 런 2층 교체 딜레마 보장을 위한 COMMON ARM_R 파츠 공급원.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: RewardScene에 첫 런 1·2층 오른팔 보장 주입

전투 보상에서 첫 런(`total_runs == 0`) 1·2층일 때 드롭 목록에 `ARM_R`가 없으면 온보딩 오른팔을 주입한다. 1층=채움, 2층=교체 딜레마.

**Files:**
- Modify: `Scenes/Dungeon/RewardScene.gd:61-100` (`_show_combat_rewards`)
- Modify: `Scripts/Validation/check_current_work_contracts.gd` (Task 2에서 만든 `_check_onboarding_arm_guarantee`에 RewardScene 검증 추가)

- [ ] **Step 1: 계약 테스트 확장 (실패 확인용)**

`Scripts/Validation/check_current_work_contracts.gd`의 `_check_onboarding_arm_guarantee()` 함수에서, RewardManager 루프 다음 `print(...)` 줄 **앞에** 아래 블록을 삽입한다:

```gdscript
	var script: GDScript = load(REWARD_SCENE_SCRIPT) as GDScript
	if script == null:
		_fail("RewardScene script failed to load (onboarding)")
		return
	var scene: Node = script.new() as Node
	_reset_state()
	# 첫 런 1층: ARM_R 없는 드롭에 주입돼야 함
	_gs.set("total_runs", 0)
	_gs.set("current_floor", 1)
	_assert_true(bool(scene.call("_is_onboarding_arm_floor")), "Floor 1 of first run not recognized as onboarding floor")
	var empty_drops: Array[PartsData] = []
	var injected: Array = scene.call("_maybe_inject_onboarding_arm", empty_drops)
	_assert_true(injected.size() == 1, "Onboarding injection did not add an arm to empty drops")
	_assert_true(injected.size() > 0 and (injected[0] as PartsData).parts_type == PartsData.PartsType.ARM_R, "Injected onboarding part is not ARM_R")
	# 첫 런 2층: 동일하게 주입
	_gs.set("current_floor", 2)
	_assert_true(bool(scene.call("_is_onboarding_arm_floor")), "Floor 2 of first run not recognized as onboarding floor")
	# 이미 ARM_R가 있으면 중복 주입 안 함
	var has_arm: Array[PartsData] = [scene_make_arm_r()]
	var unchanged: Array = scene.call("_maybe_inject_onboarding_arm", has_arm)
	_assert_true(unchanged.size() == 1, "Onboarding injection duplicated an existing ARM_R drop")
	# 3층은 온보딩 대상 아님
	_gs.set("current_floor", 3)
	_assert_true(not bool(scene.call("_is_onboarding_arm_floor")), "Floor 3 wrongly treated as onboarding floor")
	# 이미 런을 1회 마친 플레이어는 온보딩 대상 아님
	_gs.set("total_runs", 1)
	_gs.set("current_floor", 2)
	_assert_true(not bool(scene.call("_is_onboarding_arm_floor")), "Veteran run wrongly treated as onboarding floor")
	scene.free()
	_reset_state()
```

같은 파일 맨 아래(헬퍼 영역)에 테스트용 ARM_R 생성 헬퍼를 추가한다:

```gdscript
func scene_make_arm_r() -> PartsData:
	var part: PartsData = _make_part(ARM_R_PATH)
	return part
```

> 참고: `ARM_R_PATH`와 `REWARD_SCENE_SCRIPT` 상수는 이미 파일 상단에 정의되어 있다.

- [ ] **Step 2: 테스트 실패 확인**

Run: `"$GODOT" --headless --path . --script res://Scripts/Validation/check_current_work_contracts.gd`
Expected: FAIL — `_is_onboarding_arm_floor` / `_maybe_inject_onboarding_arm`가 아직 없어 `Nonexistent function` 오류.

- [ ] **Step 3: RewardScene에 보장 로직 구현**

`Scenes/Dungeon/RewardScene.gd`의 `_show_combat_rewards` 함수에서, 드롭 생성 줄을 주입 호출로 감싼다. 기존:

```gdscript
	var defeated_count: int = DungeonManager.get_last_combat_defeated_enemy_count()
	var drops: Array[PartsData] = RewardManager.generate_combat_drops(grade, defeated_count)
```

를 아래로 교체:

```gdscript
	var defeated_count: int = DungeonManager.get_last_combat_defeated_enemy_count()
	var drops: Array[PartsData] = RewardManager.generate_combat_drops(grade, defeated_count)
	drops = _maybe_inject_onboarding_arm(drops)
```

그리고 `_determine_grade(...)` 함수 정의 바로 위에 두 메서드를 추가:

```gdscript
# 첫 런 1·2층 전투 보상에 오른팔이 없으면 COMMON 오른팔을 주입한다.
# 1층=오른팔 소켓 채움, 2층=강제 교체 딜레마(파손)를 보장한다.
func _maybe_inject_onboarding_arm(drops: Array[PartsData]) -> Array[PartsData]:
	if not _is_onboarding_arm_floor():
		return drops
	for part: PartsData in drops:
		if part != null and part.parts_type == PartsData.PartsType.ARM_R:
			return drops
	var arm: PartsData = RewardManager.generate_onboarding_arm_drop()
	if arm != null:
		drops.insert(0, arm)
	return drops


func _is_onboarding_arm_floor() -> bool:
	return GameState.total_runs == 0 and (GameState.current_floor == 1 or GameState.current_floor == 2)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `"$GODOT" --headless --path . --script res://Scripts/Validation/check_current_work_contracts.gd`
Expected: PASS — `Current OK: onboarding ARM_R drop generation` 출력, 마지막 줄 `Current work contracts: PASS`.

- [ ] **Step 5: 전체 검증 통과 확인**

Run: `bash Scripts/Validation/validate.sh`
Expected: 마지막 줄 `Validation complete: PASS`. (씬 스모크·리소스 무결성 포함 전부 PASS)

- [ ] **Step 6: 커밋**

```bash
git add Scenes/Dungeon/RewardScene.gd Scripts/Validation/check_current_work_contracts.gd
git commit -m "feat: 첫 런 1·2층 오른팔 교체 딜레마 보장

첫 런 전투 보상에 COMMON 오른팔을 주입해 1층 채움 / 2층 강제 교체
딜레마를 보장. 핵심 재미(교체의 긴장) 온보딩 도달(V2) 구현.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: V5 교체 딜레마 빈도 headless 측정 스크립트

V5("런당 교체 딜레마 3회+")의 기계적 측정 가능 부분을 headless로 검증한다. 드롭 볼륨이 런당 충분한지(드롭율 0/40·1/50·2/10, 층별 적 수 기준) 시뮬레이션한다. V1(수락률)은 사람 판단 영역이므로 측정 대상에서 제외하고 문서에 명시한다.

**Files:**
- Create: `Scripts/Validation/check_swap_dilemma_frequency.gd`
- Modify: `Scripts/Validation/validate.sh:153` (검증 단계 추가)
- Modify: `Docs/superpowers/specs/2026-05-31-검증기준-온보딩-안할것-design.md` (§4 체크 + 측정 결과 주석)

- [ ] **Step 1: 측정 스크립트 작성**

`Scripts/Validation/check_swap_dilemma_frequency.gd` 생성:

```gdscript
extends SceneTree

# V5 검증: 한 런에서 교체 딜레마 "기회"가 평균 3회 이상 발생하는지 측정한다.
# 가정(문서화된 단순화):
#  - 모든 슬롯이 채워진 후반 로드아웃에서는 매 드롭이 딜레마 기회다
#    (ARM_L/ARM_R/BACK/LEG 각 소켓이 점유 상태 → 같은 타입 드롭 = 강제 교체).
#  - 따라서 딜레마 기회 수 ≈ 런 전체 전투 드롭 수.
#  - 대표 전투 경로의 층별 격파 적 수를 고정해 드롭 볼륨을 잰다.
# V1(수락률)은 사람 판단 영역이라 측정 대상이 아니다 — 설계 문서 §1 참고.

const SIM_RUNS: int = 200
const MIN_AVG_DILEMMAS: float = 3.0
# 대표 전투 경로: 1~3층 일반(적2), 6~8층 일반/엘리트(적1~2), 9층 엘리트(적1), 10층 보스(적1).
# 4~5층은 비전투 선택 가능성이 있어 보수적으로 제외.
const DEFEATED_PER_COMBAT_FLOOR: Array[int] = [2, 2, 2, 2, 2, 1, 1, 1]

var _failed: bool = false


func _initialize() -> void:
	seed(20260531)
	var reward_manager: Node = root.get_node_or_null("RewardManager")
	if reward_manager == null:
		push_error("Missing RewardManager autoload")
		quit(1)
		return
	var total_dilemmas: int = 0
	var runs_below_one: int = 0
	for _r: int in range(SIM_RUNS):
		var run_dilemmas: int = 0
		for defeated: int in DEFEATED_PER_COMBAT_FLOOR:
			var drops: Array = reward_manager.call("generate_combat_drops", PartsData.PartsGrade.COMMON, defeated)
			run_dilemmas += drops.size()
		total_dilemmas += run_dilemmas
		if run_dilemmas < 1:
			runs_below_one += 1
	var avg: float = float(total_dilemmas) / float(SIM_RUNS)
	print("V5 측정: 평균 딜레마 기회/런 = %.2f (목표 ≥ %.1f), 0회 런 = %d/%d" % [avg, MIN_AVG_DILEMMAS, runs_below_one, SIM_RUNS])
	if avg < MIN_AVG_DILEMMAS:
		push_error("V5 FAIL: 평균 딜레마 기회 %.2f < %.1f" % [avg, MIN_AVG_DILEMMAS])
		_failed = true
	if _failed:
		print("Swap dilemma frequency: FAIL")
		quit(1)
	else:
		print("Swap dilemma frequency: PASS")
		quit(0)
```

- [ ] **Step 2: 스크립트 실행 (PASS 확인)**

Run: `"$GODOT" --headless --path . --script res://Scripts/Validation/check_swap_dilemma_frequency.gd`
Expected: PASS — `V5 측정: 평균 딜레마 기회/런 = N.NN ...` 출력 후 `Swap dilemma frequency: PASS`. (드롭 기대값 ≈ 0.7/적 × 11적 ≈ 7.7 → 평균이 3을 충분히 상회)

- [ ] **Step 3: validate.sh에 검증 단계 추가**

`Scripts/Validation/validate.sh`의 `run_check "P0 combat flows" ...` 줄 **다음**에 추가:

```bash
run_check "Swap dilemma frequency (V5)" run_godot_script res://Scripts/Validation/check_swap_dilemma_frequency.gd
```

- [ ] **Step 4: 전체 검증 통과 확인**

Run: `bash Scripts/Validation/validate.sh`
Expected: 마지막 줄 `Validation complete: PASS`. 새 단계 `PASS Swap dilemma frequency (V5)` 포함.

- [ ] **Step 5: 설계 문서 §4 갱신**

`Docs/superpowers/specs/2026-05-31-검증기준-온보딩-안할것-design.md`의 §4 후속 작업에서 완료 항목을 체크하고, V1·V5 항목에 측정 결과 주석을 단다. 기존:

```markdown
- [ ] `Docs/TODO/TODO-NEXT.md`에 V1~V6 플레이테스트 항목 반영
- [ ] V1·V5 headless 시뮬레이션 가능성 검토 (휴리스틱 플레이어)
- [ ] 첫 런 2층 같은-부위 드롭 보장 로직 구현 (`RewardManager`/드롭 테이블)
- [ ] GDD §10 미결 사항에 본 설계 링크 추가
```

를:

```markdown
- [x] `Docs/TODO/TODO-NEXT.md`에 V1~V6 플레이테스트 항목 반영 (P0.5 섹션)
- [x] V5 headless 측정 — `check_swap_dilemma_frequency.gd` (런당 평균 드롭/딜레마 기회 ≥ 3 검증). **V1(수락률)은 사람 판단 영역이라 자동화 불가 — 플레이테스트로만 측정.**
- [x] 첫 런 1·2층 오른팔 교체 딜레마 보장 — `RewardManager.generate_onboarding_arm_drop()` + `RewardScene._maybe_inject_onboarding_arm()`
- [x] GDD §10 미결 사항에 본 설계 링크 추가
```

- [ ] **Step 6: 커밋**

```bash
git add Scripts/Validation/check_swap_dilemma_frequency.gd Scripts/Validation/validate.sh "Docs/superpowers/specs/2026-05-31-검증기준-온보딩-안할것-design.md"
git commit -m "test: V5 교체 딜레마 빈도 headless 측정 추가

런당 평균 드롭/딜레마 기회 ≥ 3 검증. V1 수락률은 사람 판단
영역이므로 플레이테스트 전용임을 문서화.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 완료 기준

- [ ] `bash Scripts/Validation/validate.sh` 전체 PASS (신규 V5 단계 포함)
- [ ] 첫 런 1·2층 전투 보상에 COMMON 오른팔이 보장 주입된다 (계약 테스트 PASS)
- [ ] 4개 커밋 생성 (Task 1~4)
- [ ] 설계 문서 §4 전 항목 체크 완료

## 범위 밖 (NOT DOING — 이 계획에서 하지 않음)

- 온보딩 튜토리얼 텍스트/UI 안내 (별도 작업) — 본 계획은 드롭 보장 "메커니즘"만 다룬다.
- V2/V3/V4/V6 사람 플레이테스트 실행 (계획이 아닌 실행 활동).
- 첫 런 1층 강제 장착 유도 — 플레이어 자유 보장. 보장은 "딜레마 제시"까지만.
