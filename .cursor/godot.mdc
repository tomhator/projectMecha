---
description: 프로젝트 전반 원칙 — 모든 대화에 적용
globs: ["**/*.gd", "**/*.tscn", "**/*.tres"]
alwaysApply: true
---

@docs https://docs.godotengine.org/en/stable/

# Cursor 프로젝트 규칙 래퍼

프로젝트 원본 규칙은 `Docs/AI-COLLABORATION.md`와 루트 `AGENTS.md`다.

Cursor는 이 파일을 통해 공용 규칙을 읽는다. 프로젝트 정보, 작업 절차, 검증 하네스, 커밋 전 규칙을 바꿀 때는 먼저 `Docs/AI-COLLABORATION.md`를 수정하고, 이 파일에는 Cursor 적용 조건만 유지한다.

현재 프로젝트 컨텍스트:
- PROJECT MECHA(가제)
- 메카 빌더 턴제 로그라이크
- Godot 4.x / GDScript
- `/Users/yusung/gameDev/projectMecha`

표준 검증 명령:

```bash
bash Scripts/Validation/validate.sh
```
