---
description: GDScript 파일 작업 시 Godot 4 문법·API 제약
globs: ["**/*.gd"]
---

# Cursor GDScript 규칙 래퍼

GDScript 원본 규칙은 `Docs/AI-COLLABORATION.md`의 “GDScript 규칙” 섹션과 루트 `AGENTS.md`다.

Cursor는 `**/*.gd` 작업 시 이 파일을 통해 공용 규칙을 적용한다. Godot 4.x 문법만 사용하고, 타입 명시와 `snake_case` 명명 규칙을 따른다.
